const fc = require("fast-check");
const { reverse, crappyReverse } = require("./reverse");

describe("crappyReverse", () => {
  // Put your tests here!
  // Want inspiration? Look in examples-from-slides/add.test.js
});
